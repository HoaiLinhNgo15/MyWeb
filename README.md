# Static Website: Giới thiệu HTML cơ bản

## 📌 Giới thiệu
Dự án này là một trang web tĩnh được xây dựng bằng **HTML, CSS và JavaScript** nhằm mục đích:
- Giới thiệu các thẻ cơ bản trong HTML.
- Minh họa cách sử dụng CSS để định dạng giao diện.
- Thêm một số hiệu ứng đơn giản bằng JavaScript.

## 🛠️ Công nghệ sử dụng
- **HTML5**: Tạo cấu trúc nội dung và minh họa các thẻ cơ bản.
- **CSS3**: Trang trí, bố cục, màu sắc và hiệu ứng.
- **JavaScript (ES6)**: Tạo tương tác và hiệu ứng động.

## 📂 Cấu trúc thư mục
project/
│── index.html          # Trang chính, giới thiệu các thẻ HTML cơ bản
│── style.css           # File CSS định dạng giao diện
│── script.js           # File JavaScript xử lý hiệu ứng, tương tác
│
├── assets/             # Thư mục chứa tài nguyên
│   ├── images/         # Hình ảnh minh họa (logo, banner, ví dụ HTML)
│   ├── video           # Video cần triển khai lên web
│
├──  README.md          # Tài liệu bổ sung (README.md, hướng dẫn, ghi chú)
## ✨ Nội dung trang web
Trang web sẽ minh họa các tính năng cơ bản của HTML:
- **Tiêu đề (Heading)**: `<h1> ... <h6>`
- **Đoạn văn (Paragraph)**: `<p>`
- **Liên kết (Link)**: `<a>`
- **Hình ảnh (Image)**: `<img>`
- **Danh sách (List)**: `<ul>`, `<ol>`, `<li>`
- **Bảng (Table)**: `<table>`, `<tr>`, `<td>`
- **Form cơ bản**: `<button>`

Ngoài ra:
- CSS sẽ được dùng để tạo màu sắc, bố cục, hiệu ứng hover.
- JavaScript sẽ thêm các tính năng như: thông báo khi click nút, ẩn/hiện nội dung, slideshow đơn giản.

## 🚀 Cách chạy dự án
1. Clone repository:
   ```bash
   git clone https://github.com/username/html-basic-demo.git